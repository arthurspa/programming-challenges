{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "Build C++",
            "command": "g++ \"${file}\" -g -o \"${fileDirname}/${fileBasenameNoExtension}\" -std=c++11 ",
            "type": "shell",
            "problemMatcher": []
        }
    ]
}